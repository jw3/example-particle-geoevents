geo {
  http {
    port = 9000
    port = ${?GEO_HTTP_PORT}
  }

  events {
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    properties = {
      db = "events"
      user = "postgres"
      pass = "postgres"
      host = "localhost"
    }
    numThreads = 10
  }
}
